# bun-break-noblox

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```

To test if this breaks run:

```bash
bun build --compile --outfile app index.ts && ./app
```